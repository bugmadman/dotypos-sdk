grumphp:
    tasks:
        composer:
            strict: true
        phplint:
            exclude: ['vendor']
        phpcs:
            standard: []
        phpstan:
            memory_limit: "-1"
        rector:
            clear_cache: true
